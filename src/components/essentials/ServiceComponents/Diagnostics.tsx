import { services } from "../../../assets/data/services";
import BaseComponent from "./BaseComponent";

const Diagnostics = () => {
  return <BaseComponent service={services[6]} />;
};

export default Diagnostics;
