import Header from "../components/Header";

const Home = () => {
  return (
    <>
      <Header />
      {/* <div className="grid grid-col-1 md:grid-cols-3 justify-items-center">
        {memberDetails.map((member) => (
          <Header_TeamCard member={member} />
        ))}
      </div> */}
    </>
  );
};

export default Home;
